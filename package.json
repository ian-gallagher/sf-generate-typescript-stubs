{
  "name": "typescript-sf-stub-generator",
  "version": "1.0.0",
  "description": "Generates SF API type stubs in typescript for client consumption",
  "main": "src/Main",
  "scripts": {
    "assignxperms": "sudo chmod +x ./scripts/set_execute_perms.sh && ./scripts/set_execute_perms.sh",
    "postinstall": "npm run assignxperms && npm run buildgrammar",
    "buildgrammar": "./scripts/generate_grammar_code.sh"
  },
  "author": "",
  "license": "ISC"
}
